agent_name = "gradient-bang-bot"
secret_set = "gb-bot-secrets"
agent_profile = "agent-2x"

[krisp_viva]
audio_filter = "tel"

[scaling]
min_agents = 2
max_agents = 10

[build]
context_dir = "."
dockerfile = "deployment/Dockerfile.bot"
